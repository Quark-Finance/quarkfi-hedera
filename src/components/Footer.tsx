export function Footer() {
  return (
    <footer className="border-t border-border py-5 mt-auto">
      <div className="max-w-7xl mx-auto px-8 flex items-center justify-between text-[11px] font-medium tracking-[0.5px] uppercase text-muted-foreground">
        <span>// QUARK PROTOCOL</span>
        <span>[BUILT ON HEDERA]</span>
      </div>
    </footer>
  );
}
